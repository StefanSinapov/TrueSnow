@using System.Globalization
@using TrueSnow.Data.Models
@model  IEnumerable<TrueSnow.Web.ViewModels.Comments.CommentViewModel>

@foreach (var comment in Model)
{
    <div class="row">
        <span class="col-md-1">
            <img src="~/File/@comment.Creator.AvatarId" alt="avatar" class="img-responsive img-circle" style="width: 50px;" />
        </span>
        <div class="col-md-6" style="margin-left: -35px;">
            <a href="/Profile/@comment.Creator.Id"><b>@comment.Creator.FirstName @comment.Creator.LastName</b></a>
            <small>@comment.CreatedOn.ToString("dd MMM HH:mm", CultureInfo.CreateSpecificCulture("en-US"))</small>
            <p>@comment.Content</p>
        </div>
    </div>
    <hr />
}